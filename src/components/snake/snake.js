import { useEffect } from "react";

const Snake = (props) => {
    useEffect(() => {
        if (props.canvas.current) {
            const canvas = props.canvas.current;
        }
    });
    return '';
}

export { Snake }