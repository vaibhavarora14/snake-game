import { useEffect } from 'react';

const Board = (props) => {
    useEffect(() => {
        if (props.canvas.current) {
            // TODO: draw board
        }
    })
    return '';
}

export { Board }